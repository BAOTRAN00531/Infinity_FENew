// Home.tsx
import { Button } from "../components/ui/button";

const Home = () => {
    return (
        <Button variant="default" size="default" className="bg-amber-300">
            hehe
        </Button>
    );
};

export default Home;
